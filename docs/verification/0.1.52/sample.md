# PocketWebShell 预览

这是一份给 **Markdown 全屏站点壳** 用的验收文稿。

## 能做什么

- 标题、列表、代码和引用会按文档样式渲染
- 长按正文可以走系统原生选区
- 悬浮球可以收藏以外的文档动作，例如制作应用

> 外部打开 Markdown 时，标题使用原始文件名。

```kotlin
fun hello() = "WebShell"
```

普通段落：把常用网站变成可以整理、拖动、多会话运行的口袋桌面。
